(ns main.edn.maps.template
  (:require [main.models :as model]
            [main.edn.utils :refer [prand-int prand]]
            ["@react-three/drei" :as drei]
            ["@react-three/rapier" :as rapier]))

(def world-map
  )